{
  "name": "plex-requests-api",
  "version": "0.0.0",
  "description": "api to handle requests for plex requests client",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon server.js"
  },
  "author": "Tom Daniels",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
